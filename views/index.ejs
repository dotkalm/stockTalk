<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Index Root Route</title>
    <link rel="stylesheet" type="text/css" href="/main.css">
    <script defer="defer" src="/app.js"></script>
  </head>
  <body>
    <header>
    <div class='top'>
     <% if (onlineUser) { %><a href='/users/logout'><span class='logOut'>LOG OUT</span></a>
     <a href='/users'><span class='usersLink'>USERS</span></a>
     <a href='/posts'><span class='postsLink'>POSTS</span></a>
      <% } %>
    </div>
      <h1><span class='muyBien'>Bienvenidos, <% if (onlineUser) { %><%= user.username %><% } else { %>  Amigos <% } %> </span></h1>
     </header>
    <br>
    <section>
    <% if (!onlineUser) { %>
      <a href='/users/register'><span class='register'>REGISTER</span></a>
    <% } %>
    </section>
      <main class='guys'>
      <span><%=message%></span>
      <% if (onlineUser) { %>
<h2><%= user.username %> is ONLINE
      <% } else { %> 
        <h2>Login</h2>
        <form autocomplete="off" action="/users/login" method="POST">
          <input type="text" placeholder="Username" name="username">
          <input type="password" placeholder="Password" name="password">
          <button type='submit'>Login</button>
        </form>
        
      <% } %>




    </main>
  </body>
</html>
