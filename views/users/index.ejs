<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Index Route | Users</title>
  </head>
  <body>
    <header>
      <h1>Index Route | Users</h1>
     </header>
    <main>
    
    <h2>Muy Buenos Dias, <%= user.username%>!
    <br>
    <section>
   <ul>List of Users</ul>
    <% for(let i=0; i<users.length; i++){%>
    <li>
    <a href='<%=users[i]._id%>'><%= users[i].username%></a>
    <ol> 
    <%for(let i2=0; i2<users[i].posts.length; i2++){%>
      <a href='/posts/<%=users[i].posts[i2]._id%>'> <%=users[i].posts[i2].title%> </a>


    <% } %>
    
    </ol>
    </li>
    <% } %>
    </section>
    </main>
    <footer>
    <a href='/'>return to main</a>
    </footer>
  </body>
</html>
